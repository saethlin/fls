name: CI/CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build
        runs:
          using: 'docker'
          image: 'Dockerfile'
          env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
